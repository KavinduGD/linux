# Cron

- Cron is the Linux scheduler
- It automatically runs commands or scripts at specific times.
- ` * * * * *` is the syntax for scheduling tasks in Cron, where each asterisk represents a time field (minute, hour, day of month, month, day of week).
- it runs with your user account's permissions.
